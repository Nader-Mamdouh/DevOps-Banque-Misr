apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: macarious
spec:
  selector:
    app: bookmanage
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30001
  type: NodePort